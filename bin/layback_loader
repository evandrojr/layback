#!/usr/bin/env ruby
require 'rubygems'
require 'bundler'

Bundler.require(:default, :development, :test)

system("remote_control_server -o 0.0.0.0 &")
system("watch4subs ~/Downloads pob por spa eng bre ger &")
system("crawl4subs ~/Downloads pob por spa eng bre ger &")
